# Supabase — claves públicas (browser, protegidas por RLS)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Solo para scripts de Node (seed/upload). JAMÁS con prefijo NEXT_PUBLIC_.
SUPABASE_SECRET_KEY=

# Conexión directa Postgres para migraciones (scripts/run-migrations.mjs)
DATABASE_URL=
